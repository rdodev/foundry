'use strict';

describe('MainCtrl', function() {
  
  describe('Very Useful', function() {
  	it('Super Complex', function () {
      expect(true).not.toEqual(false);
      expect(false).toBe(false);
    });
  });
});
